---
apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: assocore
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/component: web
data:
  # Frontend environment configuration
  NEXT_PUBLIC_API_URL: "https://api.assocore.localhost"
  NODE_ENV: "production"
