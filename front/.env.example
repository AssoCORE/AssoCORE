# Server-only; never exposed to the browser (no NEXT_PUBLIC_ prefix).
# Base URL the Next.js server uses to reach FastAPI server-to-server —
# the browser never calls the backend directly.
BACKEND_URL=http://localhost:8000
